import Lesson from "@/modules/course/lesson";
import { NextPage } from "next";
import React from "react";

type LessonPageProps = {};

const LessonPage: NextPage = (props: LessonPageProps) => {
  return <Lesson />;
};

export default LessonPage;
